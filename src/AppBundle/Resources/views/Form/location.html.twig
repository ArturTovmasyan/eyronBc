{% block bl_location_widget %}

    {{ form_widget(form) }}

    {# TODO: Hazarapet please send null (empty value) if remove address #}
    {#<input type="hidden" name="{{ form.vars.full_name }}" id="{{ form.vars.id }}">#}

{% endblock %}
