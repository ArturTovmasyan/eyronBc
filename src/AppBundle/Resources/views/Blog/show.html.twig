{% extends "::anp.html.twig" %}

{% block content %}
    <div class="container">
        <div  class="signup">
            <div class="row">
                <h1 class="text-center">{{ blog.title }}</h1>
            </div>
        </div>
    </div>
{% endblock %}

{%- block title -%}
    {{ blog.title }}
{%- endblock -%}

{%- block meta_description -%}
    {{- blog.metaDescription|markdown|striptags|sliceString -}}
{%- endblock -%}