{% extends "AppBundle::layout.html.twig" %}

{% block content %}

    <div class="container">
        <div class="row">
            <div class="col-sm-12">
                <h1 class="text-center"> {{ page.name }} </h1>

                {{ page.description|raw }}
            </div>
        </div>
    </div>

{% endblock %}

