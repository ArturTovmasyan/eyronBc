{% extends "AppBundle::layout.html.twig" %}

{% block javascripts %}

    {{ parent() }}
    <script type="text/javascript" src="{{ asset('bundles/app/js/images.js') }}"></script>

{% endblock %}


 {% block body %}

     {{ parent() }}

     <div>
         {{ form_errors(form) }}
         <form class="form-horizontal" method="post" action="#" enctype="multipart/form-data">

             <div class="form-group">
                 <label for="{{ form.category.vars.id }}" class="col-sm-2 control-label">{{ form_label(form.category) }}</label>
                 <div class="col-sm-10">
                     {{ form_widget(form.category, {'attr': {'class':'form-control'} }) }}
                 </div>
             </div>


             <div class="form-group">
                 <label for="{{ form.description.vars.id }}" class="col-sm-2 control-label">{{ form_label(form.description) }}</label>
                 <div class="col-sm-10">
                     {{ form_widget(form.description, {'attr': {'class':'form-control'} }) }}
                 </div>
             </div>

             {{ form_widget(form.bl_multiple_file) }}

             {#<div class="form-group">#}
                 {#<ul class="images col-sm-2 control-label" data-prototype="{{ form_widget(form.images.vars.prototype)|e }}">#}
                     {#{% if form.images.vars.value is null %}#}
                         {#{{ form_widget(form.images) }}#}
                     {#{% else %}#}
                         {#{% if form.vars.value.images|length > 0 %}#}
                             {#<ul class="file-list">#}
                                 {#{% for images in form.vars.value.images %}#}
                                     {#<li>#}
                                         {#<a class="delete-link" href="#" > Delete </a>#}
                                         {#<a href="{{ images.downloadLink }}"#}
                                            {#target="_blank">{{ images.fileOriginalName | default(images.file ? images.file.getClientOriginalName) }}</a>#}
                                     {#</li>#}
                                 {#{% endfor %}#}
                             {#</ul>#}
                         {#{% endif %}#}
                     {#{% endif %}#}
                 {#</ul>#}
             {#</div>#}

             <div class="form-group">
                 <div class="col-sm-offset-2 col-sm-10">
                     <button type="submit" class="btn btn-default">Save</button>
                 </div>
             </div>

             {{ form_widget(form._token) }}

         </form>
     </div>

 {% endblock %}
