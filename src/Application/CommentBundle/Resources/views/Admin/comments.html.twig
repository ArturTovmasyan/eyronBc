{% if admin.isGranted('VIEW', object) and admin.hasRoute('show') %}

    <a href="{{ getThreadInnerLink(object.id) }}" class="btn btn-sm btn-default view_link" title="{{ 'admin.label.name.comments'|trans() }}">
        <i class="fa fa-search-plus"></i>
    </a>

{% endif %}
