{% extends "::base.html.twig" %}

{% block confirm_email %}
{% endblock %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="col-md-6 col-md-offset-3 text-center">
                <h3>{{ 'verification.verified'|trans({}, 'FOSUserBundle') }}</h3>
                <p>{{ 'verification.verified_content'|trans({'%XXX%': project_name}, 'FOSUserBundle') }}</p>
            </div>
        </div>
    </div>
{% endblock %}