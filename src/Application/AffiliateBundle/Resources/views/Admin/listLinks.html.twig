{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{%- block field %}

    {% for link in object.links %}
        {% set slug = link|split('/') %}
        <a href="{{ link }}">{{ slug[slug|length - 1] }}</a>
        <br />
    {% endfor %}

{% endblock -%}
